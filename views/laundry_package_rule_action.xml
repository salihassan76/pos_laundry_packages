<odoo>

    <!-- Action -->
    <record id="action_laundry_package_rule" model="ir.actions.act_window">
        <field name="name">Package Templates</field>
        <field name="res_model">package.rule</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_package_rule_search"/>
    </record>

</odoo>